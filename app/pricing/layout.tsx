export const metadata = {
    title: 'Pricing - AI SDR | Transparent Pipeline & Budget Goals',
    description: 'Pricing that respects your pipeline goals and your budget. Transparent quarterly pricing with no surprises. Scale confidently with predictable costs.',
    keywords: ['AI SDR pricing', 'sales development representative cost', 'outbound automation pricing', 'automated sales development'],
};

export default function PricingLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}
